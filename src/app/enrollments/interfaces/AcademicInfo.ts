export interface AcademicInfo {
    email: string;
    CareerId: string; // Asegúrate de que los nombres coincidan con el formulario
    school: string;
    candidateType: string;
    academicLevel: string;
  } 